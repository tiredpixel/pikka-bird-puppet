# Defaults for pikka-bird-collector, sourced by the init script.
# This file is dancing on a Puppet string; it's probably not a good idea to
# change it manually.
# 
# The config variables are based on `.env.example` in the repository.


PB_CONF="<%= @path_conf %>"
PB_FORMAT=<%= @format %>
PB_INTERVAL=<%= @interval %>
PB_LOG_LEVEL=<%= @log_level %>
PB_SERVER_URI="<%= @server_uri %>"
